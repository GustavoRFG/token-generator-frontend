import React from 'react';

const Header = () => (
  <header style={{ textAlign: 'center', padding: '20px', backgroundColor: '#020D3F' }}>
    <h1>Token Creator</h1>
    <p>Create and customize your own token in minutes!</p>
  </header>
);

export default Header;
